package com.stimmax.kotlincourse.lessons.lesson03.homework

class Homework {
    // 1. Название мероприятия (неизменяемая публичная переменная, известная заранее)
    val eventName: String = "Hackaton 2025"

    // 2. Дата проведения (публичная переменная, может измениться при переносе)
    var eventDate: String = "10 ноября 2025"

    // 3. Место проведения (публичная переменная, может быть изменено)
    var eventLocation: String = "Москва, Технопарк"

    // 4. Подробный бюджет мероприятия (приватная, фиксированная переменная)
    private val detailedBudget: String = "detailedBudget"

    // 5. Количество участников (публичная, изменяемая)
    var participantsCount: Int = 0
        set(value) {
            require(value >= 0) { "Количество участников не может быть отрицательным" }
            field = value
        }

    // 6. Длительность хакатона (публичная, фиксированная)
    val eventDuration: String = "48 часов"

    // 7. Контакты и соглашения с поставщиками (приватная, изменяемая)
    private var suppliersInfo: String = "Контракт с кейтерингом"

    // 8. Текущее состояние хакатона (публичная, изменяемая)
    var eventStatus: String = "Подготовка"

    // 9. Список спонсоров (публичная, неизменяемая)
    val sponsors: String = "Список компаний"

    // 10. Бюджет мероприятия (публичная, неизменяемая, итоговая цифра)
    val budget: Int = 10000000

    // 11. Текущий уровень доступа к интернету (публичная, изменяемая)
    var internetLevel: String = "high"

    // 12. Инфо о логистике и поддержке (приватная, изменяемая)
    private var logistics: String = "План перевозки оборудования"

    // 13. Количество команд (приватная, изменяемая)
    private var teamCount: Int = 0
        set(value) {
            require(value >= 0) { "Количество команд не может быть отрицательным" }
            field = value
        }

    // 14. Перечень задач (публичная, неизменяемая)
    val tasks: String = "Задачи для участников"

    // 15. План эвакуации (публичная, изменяемая)
    var evacuationPlan: String = "Схема эвакуации"

    // 16. Список доступного оборудования (публичная, неизменяемая)
    val availableEquipment: String = "Список оборудования"

    // 17. Список свободного оборудования (публичная, изменяемая)
    var freeEquipment: String = "Список свободного оборудования"

    // 18. График питания (публичная, изменяемая)
    lateinit var mealSchedule: String

    // 19. План мероприятий на случай сбоев (приватная, фиксированная)
    private val fallbackPlan: String = "План B"

    // 20. Список экспертов и жюри (публичная, изменяемая)
    var experts: String = "Список экспертов"

    // 21. Политика конфиденциальности (публичная, фиксированная)
    val privacyPolicy: String = "Политика конфиденциальности"

    // 22. Приватные отзывы участников (приватная, изменяемая)
    private lateinit var feedback: String

    // 23. Текущая температура в помещении (публичная, изменяемая)
    var temperature: Int = 20

    // 24. Данные мониторинга сети (приватная, ресурсоемкая)
    private val networkStats: String by lazy { "Данные мониторинга" }

    // 25. Регулятор скорости интернета (приватная, изменяемая)
    private var bandwidthControl: Int = 100

    // 26. Уровень освещения (публичная, изменяемая)
    var lightLevel: Int = 80

    // 27. Лог событий (приватная, ресурсоемкая)
    private val eventLog: String by lazy { "log" }

    // 28. Возможность медпомощи (публичная, фиксированная)
    val medicalSupport: Boolean = true

    // 29. План безопасности и ЧС (приватная, фиксированная)
    private val safetyPlan: String = "Протоколы безопасности"

    // 30. Регистрационный номер (публичная, фиксированная)
    val registrationNumber: String = "REG-2025-001"

    // 31. Максимально допустимый уровень шума (публичная, фиксированная)
    val maxNoiseLevel: Int = 80

    // 32. Индикатор превышения уровня шума (публичная, изменяемая)
    var noiseExceeded: Boolean = false

    // 33. План взаимодействия с прессой (приватная, изменяемая)
    private var pressPlan: String = "План взаимодействия"

    // 34. Публичная инфо о проектах команд (публичная, изменяемая)
    var projectInfo: String = "Описание проектов"

    // 35. Статус получения разрешений (приватная, изменяемая)
    private var permissionsStatus: String = "В процессе"

    // 36. Индикатор доступности спец. оборудования (приватная, изменяемая)
    private var specialEquipmentAvailable: Boolean = false

    // 37. Список партнеров (публичная, изменяемая)
    var partners: String = "Список партнёров"

    // 38. Итоговый отчет (публичная, вычисляемая после завершения)
    val finalReport: String by lazy { "finalReport" }

    // 39. Внутренние правила распределения призов (приватная, фиксированная)
    private val prizeRules: String = "Правила распределения"

    // 40. Список призов (публичная, изменяемая)
    var prizes: String = "Призы"

    // 41. Контакты экстренных служб (приватная, фиксированная)
    private val emergencyContacts: String = "Контакты"

    // 42. Особые условия для участников с ОВЗ (приватная, фиксированная)
    private val specialConditions: String = "Условия"

    // 43. Общее настроение участников (приватная, изменяемая)
    private var mood: String = "Позитивное"

    // 44. Подробный план хакатона (приватная, инициализируется позже)
    lateinit var hackathonPlan: String

    // 45. Имя специального гостя (публичная, станет известно позже)
    lateinit var specialGuest: String

    // 46. Максимальное количество людей (публичная, фиксированная)
    val maxCapacity: Int = 500

    // 47. Количество часов для работы над проектом (публичная, фиксированная)
    val hoursPerTeam: Int = 48
}